import { CustomdirectDirective } from './customdirect.directive';

describe('CustomdirectDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomdirectDirective();
    expect(directive).toBeTruthy();
  });
});
