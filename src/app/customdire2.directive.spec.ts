import { Customdire2Directive } from './customdire2.directive';

describe('Customdire2Directive', () => {
  it('should create an instance', () => {
    const directive = new Customdire2Directive();
    expect(directive).toBeTruthy();
  });
});
